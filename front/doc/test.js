describe('Julietest', () => {

	it('Should registration', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="registrationLink"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="email"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka@gmail.com')
		cy.get('[data-test="nickname"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Kossai')
		cy.get('[data-test="username"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka')
		cy.get('[data-test="password"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('solarWindsExample')
		cy.get('[data-test="retype"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('solarWindsExample')
		cy.get('[data-test="register"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should login', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="username"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka')
		cy.get('[data-test="password"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('solarWindsExample')
		cy.get('[data-test="login"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})
	it('Should addMessage', () => {
		cy.visit('http://julie.zbynekrybicka.cz')
		cy.get('[data-test="username"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('zbynek.rybicka')
		cy.get('[data-test="password"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('solarWindsExample')
		cy.get('[data-test="login"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
		cy.get('[data-test="message"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.type('Toto je zkušební zpráva')
		cy.get('[data-test="addMessage"]:eq(' + ('' ? '' : '0') + ')')
			.should('have.length', 1)
			.and('be.visible')
			.click()
	})

})